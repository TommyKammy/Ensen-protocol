# Naming

Use stable, descriptive names for protocol artifacts.

- Use `EIP` for the Ensen Interop Protocol family.
- Use lowercase directory names for artifact groups.
- Use kebab-case for standalone Markdown documents.
- Use clear version identifiers when a schema or fixture represents a specific
  contract version.

Names must describe protocol meaning, not a specific runtime implementation.
