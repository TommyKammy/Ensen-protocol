# OpenAPI

OpenAPI specifications are deferred until EIP defines a transport-level API.

Until that boundary exists, this directory only records the deferral. Protocol
contracts may still be documented in Markdown, schemas, and fixtures without
implying an HTTP transport surface.
